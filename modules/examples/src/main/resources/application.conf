# Configuration for the persistent4s library example
# This file is used when running LibraryServer

# PostgreSQL event store configuration
persistent4s.postgres {
  host = "localhost"
  port = 5432
  user = "library"
  password = "library"
  database = "library_events"
  max-connections = 10
}
